{% extends "default.html.jinja" %}
{% block title %}
Potions
{% endblock title %}
{% block content %}
{# <section>
  <h2>Valuable Potions</h2>
  <table id="potions" data-page-length="100" class="table table-hover table-bordered align-middle text-center table-sm" data-order="[[ 4, &quot;desc&quot; ]]">
    <thead>
      <tr>
        <th>Potencies</th>
        <th>Ingredients</th>
        <th>Accessibility</th>
        <th>Price</th>
        <th>Relative value</th>
      </tr>
    </thead>
    <tbody>
      {% for potion in (data.potions | sort(reverse=true, attribute='relative_value'))[:100] %}
      
      <tr>
        <td>
          <div class="d-flex flex-wrap flex-row justify-content-center">
            {% for potency in potion.potencies %}
            <div class="px-1">
              {% include "snippets/potency/short.html.jinja" %}
            </div>
            {% endfor %}
          </div>
        </td>
        <td>
          <div class="d-flex flex-wrap flex-row justify-content-center">
            {% for ingredient in potion.ingredients %}
            <div class="px-1">
              {% include "snippets/ingredient/link.html.jinja" %}
            </div>
            {% endfor %}
          </div>
        </td>
        <td>
        {{ potion.accessibility | round | int }}
        </td>
        <td>
        {{ potion.price | round | int }}
        </td>
        <td>{{ potion.relative_value | round | int }}</td>
      </tr>

      {% endfor %}

    </tbody>
  </table>
</section> #}
<section>
  <h2>Best in slot Potions</h2>
  <table id="potions" class="table table-hover table-bordered align-middle text-center table-sm" data-order="[[ 4, &quot;desc&quot; ]]">
    <thead>
      <tr>
        <th>Potencies</th>
        <th>Ingredients</th>
      </tr>
    </thead>
    <tbody>
      {% for potion in data.useful_potions %}
      <tr>
        <td>
          <div class="d-flex flex-wrap flex-row justify-content-center">
            {% for potency in potion.potencies %}
            <div class="px-1">
              {% include "snippets/potency/short.html.jinja" %}
            </div>
            {% endfor %}
          </div>
        </td>
        <td>
          <div class="d-flex flex-wrap flex-row justify-content-center">
            {% for ingredient in potion.ingredients %}
            <div class="px-1">
              {% include "snippets/ingredient/link.html.jinja" %}
            </div>
            {% endfor %}
          </div>
        </td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
</section>
{% endblock content %}


{% block additional_scripts %}
{% set datatable_id = 'potions' %}
{% include "snippets/datatables.html.jinja" %}
{% endblock additional_scripts %}